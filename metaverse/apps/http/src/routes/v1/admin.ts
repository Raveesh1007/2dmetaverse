import { Router } from "express";

export const adminRouter = Router()
 

adminRouter.post("/element", (res, req) =>{
    
})

adminRouter.put("/element/:elementId", (res, req) =>{
    
})

adminRouter.get("/avatar", (res, req) =>{
    
})

adminRouter.get("/map", (res, req) =>{
    
})